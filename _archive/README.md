# _archive/

Historische Einmal-Artefakte, die früher das Repo-Root überfüllten:
Status-/Fortschritts-Notizen, Deployment-/Fix-Anleitungen und einmalige
Ops-Skripte (`.md`, `.sh`, `.ps1`, `.sql`).

**Kein aktiver Code.** Nichts hier wird von Diensten, Dockerfiles,
`docker-compose.yml`, `package.json` oder Anwendungscode referenziert
(verifiziert 2026-06-17). Verschoben (nicht gelöscht), um den Verlauf
nachschlagbar zu halten.

Aktuelle, gepflegte Doku liegt im Root: `README.md`, `ARCHITECTURE.md`,
`CHANGELOG.md`, `CLAUDE.md`, `ROADMAP.md` sowie in `docs/`.

Wiederherstellen einer Datei: `git mv _archive/<datei> <datei>`.
